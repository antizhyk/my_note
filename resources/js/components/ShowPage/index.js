import React from "react";
import {ShowPageWrapper} from "./styles";

const ShowPage = React.memo(() => {

    return(
        <ShowPageWrapper>
            HelloWorld!
        </ShowPageWrapper>

    )
})

export default ShowPage
